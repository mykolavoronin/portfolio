# Claude Code

Project instructions are in [`AGENTS.md`](./AGENTS.md). Follow that file.

Skills for this repo are in `.claude/skills/` (Claude Code does not load
`.agents/skills`). Same set as the shared `.agents/skills` folder.
