# Motion+ private registry (optional). Set MOTION_TOKEN in the environment to install motion-plus.
# Token must never be committed — only the ${MOTION_TOKEN} reference below.
@motionplus:registry=https://api.motion.dev/npm/
//api.motion.dev/npm/:_authToken=${MOTION_TOKEN}
